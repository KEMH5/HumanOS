spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/human_os_data
    username: postgres
    password: AnotherBanger
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        format_sql: true